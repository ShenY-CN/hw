# D 题题目分析与模型合同（M1 返工版）

## 1. 数据、结论目标与证据边界

输入为调度中心与服务区、逐箱需求与时限、运输无人机、中继无人机、通信参数、30 m DEM 和提交模板。统一单位为 m、s、kg、m³、kWh、dB。原始数据含 15 个服务区、80 个不可拆货箱、3 类 8 架运输机、14 组运输电池、2 架中继机和 6 组中继能源组件。

80 箱必须且只能在原始服务区交接一次。医疗箱满足期望送达时刻，首批保障箱满足首批截止，兼属两类时取较早者；去重后 31 箱受硬时限约束。其他货箱的期望时刻进入软及时性。Q1 输出单服务区直送组批；Q2 输出箱组、站序、机型、具体机体、电池和时刻及多目标权衡；Q3 再输出中继位置、高度、服务窗口、实体中继机和组件及连续通信证书；Q4 在固定 Q3 任务下比较两组/三组独立配置。

当前历史 Q2 解只可作为共享 warm-start 或历史基线。现有 Q3 程序在补齐“通信冲突—约束回传—重新求解”日志前，应称有限候选上的通信可行性修复与协同排程。下述 ε-约束、鲁棒选择和完整分解是待实现合同，不伪装成已完成实验。

## 2. 假设—依据—偏差—验证

| 编号 | 级别 | 假设 | 依据 | 可能偏差 | 放宽与验证 |
|---|---|---|---|---|---|
| A1 | 关键 | 货箱不可拆且只能在逐箱清单指定服务区交接 | 物资需求与配送时限、结果提交模板 | 放宽会改变题目定义 | 逐箱核对箱号、目的地和唯一交付 |
| A2 | 关键 | 航迹按爬升—巡航—下降—交接分段并满足 DEM 越障 | 30 m DEM、地理空间说明、现有物理实现 | 简化了风场与曲线绕障 | 更新 DEM/轨迹后重算时间、能耗和通信 |
| A3 | 关键 | 机体、电池、中继机和组件为实体复用资源 | 两类无人机数据表的逐架/逐组件清单 | 忽略会产生不可执行重叠 | 独立回放全部占用区间 |
| A4 | 可放宽 | 水平能耗按题给空载/满载等效航程插值，正爬升另计 | 运输机数据；Dorling 等的载荷—能量 VRP 背景 | 真实功耗受风和控制影响 | 能耗倍率、效率和储备率扫描 |
| A5 | 可放宽 | 2400 MHz 自由空间损耗加题给遮挡损耗 | 通信参数；ITU-R P.525-5 | 未显式建模天气和定位误差 | 增加附加衰减并重算裕度 |
| A6 | 可放宽 | 路线停靠数、中继网格和窗口候选有限 | 求解预算设置 | 可能漏掉更优候选 | 停靠扫描、网格加密、冲突区域扩张 |
| A7 | 可放宽 | 电池初始满电，返航后按统一曲线充满 | 设备数据与统一参数 | 初始 SOC/温度可能不同 | 改初始 SOC 和充电倍率后重排 |
| A8 | 可放宽 | Q4 固定 Q3 任务与时刻 | 当前题四口径 | 非允许重排后的全局最优 | 若重排则另建联合模型 |

## 3. Q1：单服务区直送的计数状态动态规划

对服务区 \(i\)、机型 \(g\)，将相同质量、体积和属性的箱归为 \(K_i\) 类，需求计数为 \(\boldsymbol n_i\)。批次模式 \(\boldsymbol a\) 可行当且仅当

\[
0\le a_k\le n_{ik},\quad
\sum_km_{ik}a_k\le Q_g,\quad
\sum_kV_{ik}a_k\le\bar V_g,\quad
E_{gi}(\boldsymbol a)\le(1-\rho_g)E_g^{\mathrm{use}}.
\]

\(E_{gi}\) 由固定 O01—\(i\)—O01 轨迹、DEM 越障和逐段载荷计算；装载、交接等作业时间进入航次时长，但当前运输能耗模型不据此额外虚构地面作业耗电。连续载荷二分只用于快速排除；最终以完整模式回算为准。全部非零可行模式构成 \(\mathcal A_i\)。

令 \(\lambda_{ig\boldsymbol a}\in\mathbb Z_{\ge0}\) 为模式使用次数：

\[
\min_{\mathrm{lex}}\left(
\sum_{g,\boldsymbol a}\lambda_{ig\boldsymbol a},
\sum_{g,\boldsymbol a}E_{gi}(\boldsymbol a)\lambda_{ig\boldsymbol a}
\right),
\quad
\sum_{g,\boldsymbol a\in\mathcal A_i}a_k\lambda_{ig\boldsymbol a}=n_{ik}.
\]

定义 \(F_i(\boldsymbol r)\) 为完成剩余计数 \(\boldsymbol r\) 的最优（架次，能耗）：

\[
F_i(\boldsymbol0)=(0,0),
\]

\[
F_i(\boldsymbol r)=\min_{\mathrm{lex}}
\{F_i(\boldsymbol r-\boldsymbol a)+(1,E_{gi}(\boldsymbol a)):
(g,\boldsymbol a)\in\mathcal A_i,\ \boldsymbol a\le\boldsymbol r\}.
\]

无可行转移则取 \((+\infty,+\infty)\)。保存前驱恢复方案。完整枚举可行整数模式时，对 \(\sum_kr_k\) 归纳可证 Bellman 最优性，故在“单点直送、固定轨迹和能耗模型”内词典序全局最优；该证明不外推到 Q2 多站与实体排程。二分容差、DEM 分辨率和能耗假设是数值边界。

## 4. Q2：概念完整模型与实际求解层

### 4.1 概念航次槽模型

本层用 \(k\in\mathcal K\) 表示未确定内容的概念航次槽；实现层的完整路线列另用 \(r\in\mathcal R\)。变量域：

\[
a_k,x_{bk},h_{ki},v_{kij},y_{kg},z_{ku},w_{kr}\in\{0,1\},
\quad s_k,C_k,D_b\ge0,\quad o_{ki}\in[0,|\mathcal N|].
\]

\[
\sum_kx_{bk}=1,\quad x_{bk}\le a_k,\quad
x_{bk}\le h_{k,n(b)},\quad
h_{ki}\le\sum_{b:n(b)=i}x_{bk}.
\]

仓库度、流守恒和子回路：

\[
\sum_{j\in\mathcal N}v_{k0j}=a_k=\sum_{i\in\mathcal N}v_{ki0},
\quad
\sum_jv_{kij}=h_{ki}=\sum_jv_{kji},
\]

\[
o_{ki}-o_{kj}+|\mathcal N|v_{kij}
\le|\mathcal N|-1+|\mathcal N|(2-h_{ki}-h_{kj}).
\]

机型、机体、电池唯一分配：

\[
\sum_gy_{kg}=a_k,\quad
\sum_{u\in\mathcal U_g}z_{ku}=y_{kg},\quad
\sum_{r\in\mathcal R_g}w_{kr}=y_{kg}.
\]

\[
\sum_bm_bx_{bk}\le\sum_gQ_gy_{kg},\quad
\sum_bV_bx_{bk}\le\sum_g\bar V_gy_{kg},\quad
E_k\le\sum_g(1-\rho_g)E_g^{\mathrm{use}}y_{kg}.
\]

\[
C_k=s_k+\Delta_k(x,v,y).
\]

用求解器的 enforcement literal \(x_{bk}=1\) 表达条件交付；若写成大 \(M\)，则 \(M\) 取调度时间域上界与最早可达时刻之差的有效上界，而不是任意超大常数：

\[
D_b\ge s_k+\tau_{kb}(x,v,y)-M(1-x_{bk}),\quad
D_b\le s_k+\tau_{kb}(x,v,y)+M(1-x_{bk}).
\]

对 \(b\in\mathcal H\)，\(D_b\le h_b\)；非医疗箱 \(T_b\ge D_b-d_b,\ T_b\ge0\)。每个 \((k,u)\) 建立存在变量 \(z_{ku}\) 的 optional flight interval \(I^U_{ku}=[s_k,C_k)\)；每个 \((k,r)\) 建立存在变量 \(w_{kr}\) 的 optional battery interval \(I^B_{kr}=[s_k,C_k+q_{kr})\)。对具体资源分别 NoOverlap；未启用槽固定 \(s_k=C_k=0\)。

### 4.2 实际路线列与固定路线 CP-SAT

实际路线列

\[
r=(B_r,\sigma_r,g_r,E_r,\Delta_r,\{\tau_{rb}\}_{b\in B_r})
\]

已固定箱集、站序和机型。当前启发式直接搜索覆盖 80 箱的完整路线列集合 \(\mathcal R^c\)，并未求解列生成主问题。给定 \(\mathcal R^c\) 后，CP-SAT 只决定 \(s_r,C_r\) 及机体/电池：

\[
C_r=s_r+\Delta_r,\quad D_b=s_r+\tau_{rb},\quad
\sum_{u\in\mathcal U_{g_r}}z_{ru}=1,\quad
\sum_{q\in\mathcal R_{g_r}}w_{rq}=1.
\]

optional intervals 的存在变量分别为 \(z_{ru}\) 和 \(w_{rq}\)。概念槽 \(k\) 不得与路线列 \(r\) 混称。若未来实现路线列选择，另设 \(\lambda_r\) 并满足 \(\sum_{r:b\in B_r}\lambda_r=1\)。

### 4.3 ε-约束、裕度与归档解

\[
T_\pi=\sum_{b\notin\mathcal M}\pi_bT_b,\quad
C_T=\max_kC_k,\quad E_T=\sum_kE_k,\quad N_T=\sum_ka_k.
\]

启用航次能源裕度 \(r_k^E\) 为允许耗能减实际耗能再除电池能量。用

\[
\gamma_E\le r_k^E+M_E(1-a_k)
\]

保证最小值只在启用航次取得。先求 \(T_\pi^*\)，加 \(T_\pi\le T_\pi^*+\varepsilon_T\)，再对 \(C_T,E_T,N_T,-\gamma_E\) 生成非支配前沿。归档解 \(S^{\mathrm{arc}}\) 只作所有适用算法共享初值或基线。正式实验固定有效评估数、至少 10 个种子并保存统计与来源。

## 5. Q3：中继资源、连续证书与分解协调

### 5.1 中继启用和资源

对任务槽 \(\ell\)、候选站点—高度 \(h\)、中继机 \(u\)、组件 \(r\)，定义 \(e_\ell,q_{\ell h},z^R_{\ell u},w^R_{\ell r}\in\{0,1\}\)：

\[
\sum_hq_{\ell h}=e_\ell,\quad
\sum_uz^R_{\ell u}=e_\ell,\quad
\sum_rw^R_{\ell r}=e_\ell.
\]

令 \(t_\ell,a_\ell,b_\ell,c_\ell\) 为开始、服务开始、服务结束、返航时刻。以下时序和能量等式仅在 \(e_\ell=1\) 时强制，可由 CP-SAT 的 OnlyEnforceIf 实现：

\[
a_\ell=t_\ell+t^{\mathrm{prep}}+\sum_hq_{\ell h}\tau_h^{\mathrm{out}}+t^{\mathrm{link}},
\quad
c_\ell=b_\ell+\sum_hq_{\ell h}\tau_h^{\mathrm{ret}}.
\]

\[
0\le t_\ell\le a_\ell\le b_\ell\le c_\ell\le H
\qquad(e_\ell=1).
\]

当 \(e_\ell=0\) 时固定

\[
t_\ell=a_\ell=b_\ell=c_\ell=E^R_\ell=0.
\]

等价的大 \(M\) 写法需将上述时序、飞抵/返航等式和能量等式的双向误差均限制在 \(M(1-e_\ell)\) 内，并加 \(0\le t_\ell,a_\ell,b_\ell,c_\ell\le He_\ell\)。占机区间 \([t_\ell,c_\ell+t^{\mathrm{turn}})\)、服务区间 \([a_\ell,b_\ell]\)、组件区间 \([t_\ell,c_\ell+q^R_\ell)\) 分别以 \(z^R_{\ell u},e_\ell,w^R_{\ell r}\) 为 optional interval 存在变量；具体机体和组件 NoOverlap。未启用任务不存在任何区间。

\[
E^R_\ell=\sum_hq_{\ell h}E_h^{\mathrm{fly}}
+(b_\ell-a_\ell+t^{\mathrm{link}})P^{\mathrm{hover+comm}},
\qquad
E^R_\ell\le(1-\rho_R)E_R^{\mathrm{use}}e_\ell.
\]

能量等式在 \(e_\ell=1\) 时生效，\(e_\ell=0\) 时由上述固定式令其为 0。若代码已把建链计入服务时间，建链能量只能计一次。

### 5.2 接入—回程—在役逻辑与全覆盖

对运输区间 \((k,m)\)，子问题返回 \(\kappa^{\mathrm{dir}}_{km}\)、\(\kappa^{\mathrm{acc}}_{kmh}\)、\(\kappa^{\mathrm{back}}_h\)。证书损耗上界

\[
\bar L_I=32.45+20\log_{10}f_{\mathrm{MHz}}
+20\log_{10}\bar D_{I,\mathrm{km}}+\bar B_I
\]

不超过允许损耗才令对应 \(\kappa=1\)。距离上界取两端最大距离乘 1.0002；遮挡用题给 10 dB 或 DEM 视线扫掠证明；不能证明则二分，深度用尽返回冲突。

定义直连 \(d_{km}\) 和中继覆盖 \(c_{km\ell}\)：

\[
d_{km}\le\kappa^{\mathrm{dir}}_{km},
\]

\[
c_{km\ell}\le e_\ell,\quad
c_{km\ell}\le\sum_hq_{\ell h}\kappa^{\mathrm{acc}}_{kmh},\quad
c_{km\ell}\le\sum_hq_{\ell h}\kappa^{\mathrm{back}}_h.
\]

若区间为 \([\underline t_{km},\overline t_{km}]\)，则

\[
a_\ell\le\underline t_{km}+M(1-c_{km\ell}),\quad
b_\ell\ge\overline t_{km}-M(1-c_{km\ell}).
\]

全覆盖正式约束：

\[
d_{km}+\sum_\ell c_{km\ell}\ge1,\qquad\forall k,m.
\]

同一个 \(\ell\) 同时承担接入与回程，不能拼接不同中继。离散采样不代替连续证书。

### 5.3 分解和目标

主问题提出运输/中继候选；通信子问题返回冲突；覆盖割要求改变路线/机型/时刻或启用可覆盖中继；无候选则加密站点/高度/窗口或生成 split、relocate、站序邻域。保存每轮冲突、割、扩张、目标和停止原因。

\[
(C_J,E_T+E_R,N_T,N_R,-\gamma_E^T,-\gamma_E^R,-\gamma_L),\qquad
\gamma_L=\min_{I\in\mathcal C}\mu_I.
\]

其中 \(\gamma_E^T\) 只在启用运输航次取最小相对能源裕度，\(\gamma_E^R\) 只在启用中继任务取最小相对能源裕度；若需单一联合指标，先无量纲化后定义 \(\gamma_E^{J}=\min(\gamma_E^T,\gamma_E^R)\)。同时报告高度余量 \(\gamma_H\)，不擅自规定题目未给的 dB 门槛。

## 6. Q4：组件分组—资源峰值—词典序配置

固定 Q3 任务。对不可拆组件 \(c\) 和组 \(h\)，\(a_{ch}\in\{0,1\}\)：

\[
\sum_{h=1}^Ka_{ch}=1,\qquad \sum_ca_{ch}\ge1.
\]

用“组件 1 属于组 1，且各组最小组件编号递增”消除标签对称；递归枚举等价地只允许放入已有组或首个新组。

设任务 \(j\) 属于组件 \(c(j)\)，对资源 \(k\) 的需求为 \(d_{jk}\)，占用区间为 \(I_j\)。在区间端点事件集 \(\mathcal T\) 上定义峰值：

\[
r_{hk}\ge\sum_jd_{jk}a_{c(j),h}\mathbf1\{t\in I_j\},
\qquad t\in\mathcal T.
\]

由于后续词典序目标最小化总配置 \(\sum_{h,k}r_{hk}\)，最优解中 \(r_{hk}\) 自动取上述所有事件时刻并发需求的最大值，即峰值等式成立；若脱离该最小化目标使用本约束，应显式定义 \(r_{hk}=\max_{t\in\mathcal T}\{\cdots\}\)。电池/组件区间到充满，中继机区间含周转；服务某组运输区间的中继任务必须在该组独立配置。

令 \(R_k=\sum_hr_{hk}\)，统一池峰值 \(r_k^{\mathrm{pool}}\)，库存 \(A_k\)：

\[
\mathrm{duplicated}_k=R_k-r_k^{\mathrm{pool}},\quad
\mathrm{unused}_k=\max(0,A_k-R_k),\quad
\mathrm{deficit}_k=\max(0,R_k-A_k).
\]

字段名与代码统一使用 duplicated、unused、deficit。正式目标：

\[
\min_{\mathrm{lex}}\left(
\sum_k\mathrm{deficit}_k,\ \sum_kR_k,\
\operatorname{CV}(W_1,\ldots,W_K)\right).
\]

即缺口→配置规模/重复冗余→工作量 CV。由于 pooled 为常数，配置总量和重复冗余排序等价。另保留缺口→CV→配置的均衡备选，但不得混称正式方案。

## 7. 待实现接口、来源与失败条件

待接口：Q2 ε-约束前沿与统一 warm-start 多种子协议；Q3 显式回程证书和分解迭代日志；Q4 输出 pooled、duplicated、unused、deficit。验收包括 80/80 正确交付、硬违约 0、具体资源无重叠、全通信区间证书和最紧裕度定位。

可追溯来源：题目七项附件；Dorling et al., 2017, DOI 10.1109/TSMC.2016.2582745；Zhang et al., 2021, DOI 10.1016/j.trd.2020.102668；ITU-R P.525-5, 2024。

若 ε-约束未产生多个互不支配解，应如实报告；区间不能连续证明时，采样通过也不能判可行；Q3 无冲突回传循环时必须降级表述。作者完成本轮自检，按要求不自行发起 M1。
