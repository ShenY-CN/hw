\section{问题二：异构多航次运输与共享资源调度}

问题一在“每架次仅服务一个服务区、且忽略实体机与共享电池”的假设下给出了运输能力基准与逐区组批方案。问题二同时放开两重限制：空间上允许同一架次依次访问多个服务区并逐站扣减载荷，资源上要求把 80 个货箱落实到 8 架实体运输无人机与 14 组共享电池上，并满足医疗物资与首批保障货箱的时限要求。允许多站访问并引入实体资源后，货箱分配、访问顺序、机型选择、机体、电池和起飞时刻相互耦合。路线决定能耗与到达时刻，到达时刻决定时限是否满足，能耗与充电时间又决定机体和电池能否被后续架次复用。

\subsection{问题分析与建模思路}

\subsubsection{对问题一的继承与新增}

本问沿用的物理口径已在第2、3章给出：航段以两节点水平直线为投影，巡航海拔取沿线DEM最高地面以上50~m，交接后从该站作业高度重新爬升；航段飞行时间与能耗按式~\eqref{eq:range}--\eqref{eq:energy}计算；每个架次总耗电不超过$(1-\rho_g)E_g^{\rm use}$，货箱不可拆分且逐站扣重。相对问题一，本问新增三点：其一，访问顺序成为决策变量，单架次可服务多个服务区；其二，引入“航次—机型—机体—电池”四层指派与占用日历；其三，时限按语义分层，31 箱硬截止（医疗 16 箱与首批 30 箱去重后为 $16+30-15=31$ 箱）、其余 64 箱按优先系数计软迟到。

由此带来三个难点。{\heiti 一是组合规模}：24 个架次、平均每架次约 3.3 箱，箱划分、站序、机型、机体与电池、起飞时刻五类决策耦合，属带时间窗的异构多航次车辆路径问题。{\heiti 二是资源时序约束}：电池遵循“使用—充电—再用”（等效充满时间对 A、B、C 型分别为 1800、2400、3000~s），机体另受准备 300~s 与逐箱装载 30~s 约束，故一个架次能否执行同时取决于自身耗电与电池是否已充满，架次之间并非独立。{\heiti 三是时限与指标不可归一}：医疗与首批箱是硬承诺，混入软惩罚会让最优解倾向牺牲关键箱；而及时性（系数·s）、收尾（s）、能耗（kWh）与架次（次）量纲不同，直接加权必须人为设定汇率。

\subsubsection{建模框架与求解思路}

针对上述难点，本文采用三层结构：{\heiti 物理层}把题给航程、能耗、交接与充电规则写成可调用的成本函数，不含优化决策；{\heiti 模型层}用 0--1 变量与连续时间把问题写成混合整数规划（式~\eqref{eq:q2-deadline}--\eqref{eq:q2-margin}），用于完整描述货箱分配、路径、机型、资源和时间约束，并作为后续启发式搜索的可行性判据；{\heiti 算法层}用路线搜索与固定航次 CP-SAT 的两层分解，在给定预算内比较候选。时限按语义分层，硬截止进入可行性，期望时刻只进入软指标；目标不加权成单一分数。完整模型用于描述约束关系，实际求解则采用分层算法。所报主方案取自归档基线，是经独立验证的可执行方案，不据此声称完整组合空间上的全局最优。

\subsection{配送时限、决策变量与优化目标}

本问沿用第 4 章符号，不重复定义公共符号，只在首次出现处补充航段序号 $k$、架次总耗时 $\Delta_p$、货箱自起飞至交接完成的时间 $\tau_{pb}$ 与优先加权平均送达时刻 $\bar{T}^{\rm arr}_\omega$。在完整模型中，$p$ 表示潜在航次；实际算法中，一条路线由箱集、访问顺序和机型共同确定，记为 $p=(B_p,\pi_p,g_p,E_p,\Delta_p,\{\tau_{pb}\})$。

\subsubsection{硬截止与软迟到的生成}

每个货箱 $b$ 在附件中可能同时带有期望送达时刻 $T_b$ 与首批截止时刻 $f_b$。出于救援优先级考虑，本文按“就紧”原则生成其适用硬截止：

\begin{equation}
D_b=
\begin{cases}
\min(T_b,f_b), & b\ \text{兼属医疗与首批},\\
T_b, & b\ \text{仅属医疗},\\
f_b, & b\ \text{仅属首批},\\
+\infty, & \text{其他},
\end{cases}
\qquad T_b^{\rm arr}\le D_b\quad (b\in\mathcal{H}).
\label{eq:q2-deadline}
\end{equation}

按附件逐箱清单统计，80 箱的构成与时限归属如表~\ref{tab:box-composition} 所示。首批保障货箱共 30 箱，由 15 箱医疗物资与 15 箱饮用水构成；其中 15 箱医疗物资已计入医疗集合，故去重后受硬截止约束的货箱为 $16+30-15=31$ 箱。是否兼属直接决定取较早时刻，这一处理避免了同一箱在两套时限下出现宽松解释；按同一清单重算，80 箱合计 758~kg、2.011~m$^3$，与附件汇总值一致，说明时限表与逐箱清单位于同一口径下。

\begin{table}[htbp]
\centering
\caption{货箱型构成与时限归属}
\label{tab:box-composition}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lccccc p{1.7cm}}
\toprule
{\heiti 物资类型} & {\heiti 箱数} & {\heiti 单箱质量/kg} & {\heiti 单箱体积/m}$^3$ & {\heiti 优先系数} & {\heiti 首批箱数} & {\heiti 是否受硬截止} \\
\midrule
医疗物资 & 16 & 3 & 0.012 & 24/22/20/16 & 15 & 是（期望时刻） \\
饮用水 & 36 & 14 & 0.027 & 12/10/8 & 15 & 是（首批截止） \\
应急食品 & 19 & 8 & 0.028 & 10/8/6 & 0 & 否 \\
生活卫生用品 & 9 & 6 & 0.035 & 8/6/4 & 0 & 否 \\
\midrule
合计 & 80 & --- & --- & --- & 30 & 去重后 31 \\
\bottomrule
\end{tabular}
\end{table}

除 15 箱受首批截止约束的饮用水外，非医疗货箱的期望时刻只进入软指标，其迟到量定义为

\begin{equation}
L_b=\max\bigl(0,\;T_b^{\rm arr}-T_b\bigr).
\label{eq:q2-late}
\end{equation}

全部非医疗箱的加权迟到 $\bar{L}_\pi=\sum_{b\notin\mathcal{M}}\omega_bL_b$ 即及时性指标，见式~\eqref{eq:q2-metrics}。其中 $\omega_b$ 取附件给出的应急优先系数：医疗物资 24 或 22、20、16，饮用水 12 或 10、8，应急食品 10 或 8、6，生活卫生用品 8 或 6、4。优先系数只出现在软指标中。硬截止不设权重：该约束一旦被违反，方案即不可行。

\subsubsection{决策变量与目标指标}

完整模型以 $a_p$、$x_{bp}$、$h_{pi}$、$v_{pij}$ 表示航次启用、箱分配、服务区访问与路径弧，以 $y_{pg}$、$z_{pu}$、$w_{pr}$ 表示机型、实体机与电池的三层指派，并引入架次开始时刻 $s_p$、返航结束时刻 $e_p$ 与架次能耗 $E_p$，构成式~\eqref{eq:q2-assign}--\eqref{eq:q2-capacity}。在此基础上定义四个评价量：

\begin{equation}
\bar{L}_\pi=\sum_{b\notin\mathcal{M}}\omega_bL_b,\qquad
C_T=\max_{p}e_p,\qquad
\bar{T}^{\rm arr}_\omega=\frac{\sum_b\omega_bT_b^{\rm arr}}{\sum_b\omega_b},\qquad
E_T=\sum_{p}E_p,
\label{eq:q2-metrics}
\end{equation}

以及刻画方案离能量上限还有多远的最小能源裕度

\begin{equation}
\gamma_E=\min_{p:\,a_p=1}\frac{(1-\rho_{g_p})E_{g_p}^{\rm use}-E_p}{E_{g_p}^{\rm use}}.
\label{eq:q2-margin}
\end{equation}

与绝对耗电相比，$\gamma_E$ 是归一化后的相对量，可在不同机型之间比较哪一个架次最先接近返航储备下限。路线搜索的默认排序为硬约束违反、加权迟到 $\bar{L}_\pi$、收尾时间 $C_T$、优先加权平均送达 $\bar{T}^{\rm arr}_\omega$、总能耗 $E_T$、架次数 $N_T$ 和能源裕度 $-\gamma_E$。及时性、能耗、架次和稳健四种配置，在加权迟到与收尾时间的容忍约束下改变后续次序，不把不同量纲的指标加权求和。四法对比汇总时比较加权迟到、收尾时间、总能耗、架次数和能源裕度。主方案取自此前归档的基线，选取理由见后文实验比较。

\subsection{异构无人机多航次路径与调度模型}

该问题属于带时间窗的异构多航次车辆路径问题。以下约束描述货箱分配、路径、机型、载荷、时间与资源周转，其中 $p$ 表示潜在航次。

{\heiti（1）箱分配与访问一致性。}
\begin{equation}
\sum_p x_{bp}=1,\qquad
x_{bp}\le a_p,\qquad
x_{bp}\le h_{p,a(b)},\qquad
h_{pi}\le\sum_{b:\,a(b)=i}x_{bp}.
\label{eq:q2-assign}
\end{equation}
第一式对应货箱不可拆分；第二式保证只有已启用航次才能承运；第三式使箱分配以其目的服务区被访问为前提；第四式排除访问某服务区却无箱可交的空访问，从而保证访问变量与货箱分配保持一致。

{\heiti（2）路径流守恒与子回路消除。}
\begin{align}
&\sum_{j\in N}v_{p0j}=a_p=\sum_{i\in N}v_{pi0},\qquad
\sum_j v_{pij}=h_{pi}=\sum_j v_{pji},\nonumber\\
&o_{pi}-o_{pj}+|N|v_{pij}\le|N|-1+|N|\bigl(2-h_{pi}-h_{pj}\bigr).
\label{eq:q2-route}
\end{align}

第一行要求每个航次以 O01 为起点与终点、每个被访问站入弧数等于出弧数等于 1，即为一条简单回路；第二行是 Miller--Tucker--Zemlin 型次序约束，当弧 $i\to j$ 被使用且两站均被访问时强制 $o_{pj}\ge o_{pi}+1$，排除与 O01 脱离的小回路。由于本题允许一架次访问多站，若不显式消除子回路，求解器极易把多条互不相连的短回路误判为可行。

{\heiti（3）机型、机体与电池的三层指派。}
\begin{equation}
\sum_g y_{pg}=a_p,\qquad
\sum_{u\in\mathcal{U}_g}z_{pu}=y_{pg},\qquad
\sum_{r\in\mathcal{Q}_g}w_{pr}=y_{pg},
\label{eq:q2-resource}
\end{equation}

即每个启用航次恰选一种机型，所用实体机与电池都必须属于该机型（$\mathcal{U}_g$、$\mathcal{Q}_g$ 分别为机型 $g$ 的实体机集合与共享电池集合）。这使得题面“同一机型电池可跨机调度、不同机型不可混用”被写成下标约束，是离散资源在模型中的唯一入口。

{\heiti（4）载重、容积与逐段载荷。}
\begin{equation}
\sum_b M_bx_{bp}\le\sum_g Q_gy_{pg},\qquad
\sum_b V_bx_{bp}\le\sum_g V_gy_{pg},\qquad
E_p\le\sum_g(1-\rho_g)E_g^{\rm use}y_{pg}.
\label{eq:q2-capacity}
\end{equation}

前两式作用于整个航次的箱集而非单站箱集，因为多站架次的载重峰值出现在尚未交付任何箱的起飞时刻；第三式把“同一路线换用不同机型是否仍安全”直接写进约束。与之配套，逐段剩余载荷按尚未交付箱重之和更新：
\begin{equation}
q_p^{k}=\sum_{\substack{b:\,x_{bp}=1,\ a(b)\in\pi_p\ \text{中第}\ k\ \text{段及之后}}}\!\!M_b,
\label{eq:q2-payload}
\end{equation}
其中 $k$ 为航段序号、$\pi_p$ 为访问序列，即每交付一站立即扣重，再以更轻载荷计算后续航段。

{\heiti（5）时间推进。}令 $e_p=s_p+\Delta_p$，并在 $x_{bp}=1$ 时强制 $T_b^{\rm arr}=s_p+\tau_{pb}$，其中 $\Delta_p$ 为架次总耗时、$\tau_{pb}$ 为该箱自起飞至交接完成的时间。站点交接时间按基础交接加逐箱增加计（A、B 型 $150+30n_i$~s，C 型 $180+36n_i$~s，$n_i$ 为交接箱数），本站全部货箱统一记为交接结束时刻，于是
\begin{equation}
t_{p,i+1}=t_{p,i}+\bigl(t^{\rm hand}+n_it^{\rm box}\bigr)+t^{g_p}_{i,i+1},
\label{eq:q2-time}
\end{equation}
其中 $t^{g}_{i,i+1}$ 为式~\eqref{eq:energy}同组的航段飞行时间。本站全部货箱统一记为交接结束时刻送达。实际逐箱交付可能在交接过程中提前完成，因此这一记账方式不会夸大时限裕度，是一种保守处理。

{\heiti（6）机体与电池的周转闭合。}同一架实体机与同一组电池都要被多个航次复用，故对每架机体、每组电池设置以 $z_{pu}$、$w_{pr}$ 为存在变量的可选区间并施加 NoOverlap：
\begin{equation}
[s_p,\,e_p)\ \text{两两不重叠}\ (u\ \text{相同}),\qquad
\bigl[s_p,\ e_p+t^{\rm chg}_{g_p}\bigl(1-E_p/E^{\rm use}_{g_p}\bigr)\bigr)\ \text{两两不重叠}\ (r\ \text{相同}).
\label{eq:q2-turnover}
\end{equation}
电池区间被延长至充满，因为再次投入前须恢复 100\%，而充电时段同样占用该组件。由于 $t^{\rm chg}$ 取决于任务结束时的荷电状态，而荷电状态又由架次能耗决定，充电时长与路线能耗相互关联。本文采用可选区间变量与 NoOverlap 约束描述机体、电池及充电周转；早返航的机体会为后续架次留出更多复用时间。

至此式~\eqref{eq:q2-assign}--\eqref{eq:q2-capacity}、\eqref{eq:q2-payload}、\eqref{eq:q2-time}、\eqref{eq:q2-turnover}共同构成完整可行域。但该模型不宜直接求解：以 30 个候选航次估算，$v_{pij}$ 约 $30\times16\times16\approx7.7\times10^3$ 个、$x_{bp}$ 约 $2.4\times10^3$ 个，另有三层指派变量、连续时刻与全部资源区间变量；变量既含 0--1 决策又含连续时间，且式~\eqref{eq:q2-turnover}把离散路线与充电时长耦合。因此下节按“路线”与“排程”两个子结构分解。

\subsection{路线搜索与固定航次调度}

给定运输路线后，资源排程可作为独立子问题求解，因此本文将路线生成与实体资源调度分层处理。给定一条完整路线——箱集 $B_p$、访问序列 $\pi_p$、机型 $g_p$ 及其决定的 $\Delta_p$、$E_p$ 与 $\{\tau_{pb}\}$——剩余问题是为该航次安排开始时刻并分配机体与电池，变量规模显著降低，可由约束规划求解。上层搜索负责路线组合，下层求解负责时间与资源分配，接口为：

\begin{itemize}
  \item 上层输出：完整路线列 $p=(B_p,\pi_p,g_p,E_p,\Delta_p,\{\tau_{pb}\})$；
  \item 下层输入：该路线列与机体、电池日历；
  \item 下层输出：开始时刻 $s_p$、机体 $u_p$、电池 $r_p$，以及 $e_p=s_p+\Delta_p$ 与 $T_b^{\rm arr}=s_p+\tau_{pb}$。
\end{itemize}

下层若判定某路线列在任何开始时刻下都无法满足资源日历或硬时限则判其不可行并回传，上层据此继续搜索。路线列由随机化构造（独立重建方案）与共享五类邻域的局部改造产生：调整航次次序（改变资源复用顺序）、整箱跨航次搬移（均衡载重）、单箱拆成直送（压缩关键箱到达时刻）、更换机型或重排站序（在载重、容积与能耗间换取空间）、合并两个航次（减少架次但可能触碰能量上限）。每次最多生成 35 次物理可行变换，凡违反载重、容积、能量或硬时限者生成阶段即被剔除。三种局部搜索共用这五类邻域而区别在接受准则：局部爬山每轮考察 10 个邻居、连续 8 轮无改进即回退；禁忌搜索每轮 10 个邻居、禁忌期限 12 轮并允许“优于全局最好解则破禁”；模拟退火最低温度 0.001、按已评估比例线性降温、词典序相对损失按 0.02 尺度进入接受概率。上述参数均取自实际运行配置，而非事后调参。

下层在路线固定后只保留排程部分：
\begin{equation}
e_p=s_p+\Delta_p,\qquad
T_b^{\rm arr}=s_p+\tau_{pb},\qquad
\sum_{u\in\mathcal{U}_{g_p}}z_{pu}=1,\qquad
\sum_{r\in\mathcal{Q}_{g_p}}w_{pr}=1,
\label{eq:q2-schedule}
\end{equation}

即每条路线列恰占一架同型机体与一组同型电池。配合式~\eqref{eq:q2-turnover}的区间不重叠与式~\eqref{eq:q2-deadline}的硬截止，下层成为含资源日历与时间窗的排序问题，其决策变量只有开始时刻与两类资源编号，故可由 CP-SAT 精确求解或证明不可行。这也是本文能够对每个候选路线给出确定结论的原因：路线层是启发式的，排程层不是。

\subsection{算法对比设置与结果范围}

为在相同条件下比较四类搜索方法，设置如下：随机化构造、局部爬山、模拟退火与禁忌搜索四种方法各用 10 个随机种子，共 40 次运行；{\heiti 每个种子独立随机构造一个初始方案，同一初始方案由四种方法共用}，以消除初值差异；每个种子的有效候选上限为 30，达到上限即停止；墙钟保护取 90~s（另设 1.2~s 排程保护），仅用于防止个别实例异常耗时，不作为预算分配依据。搜索在及时性、能耗、架次、稳健四种目标配置下分别进行，并统一施加容忍规则：每个种子的加权迟到不超过其初始方案的值，且收尾时间不超过初始值的 1.10 倍。

历史 24 架次方案不参与搜索，而作为{\heiti 外部基线}参与比较，其指标为：24 架次、总能耗 71.9169~kWh、收尾 6276.83~s、优先加权平均送达 45.69~min、加权迟到 0、最低能源裕度 0.004065。40 次运行全部可行（每箱均送达、无硬截止违约），且无一次触发墙钟保护。表~\ref{tab:q2_formal_protocol}给出四法在 10 个种子上的均值与标准差。

\input{tables/q2_formal_protocol}

由表~\ref{tab:q2_formal_protocol}可见，四种方法的表现差别明显：随机化构造的平均架次最少（23.7$\pm$0.9，最优 22 架次），但平均收尾最长（9551$\pm$1027~s）；局部爬山与禁忌搜索的平均收尾较短（约 8490$\sim$8528~s）；模拟退火的能耗与架次波动最大。四法均有种子达到零加权迟到，也均出现过迟到较大的种子，说明及时性对初始方案与搜索方向较为敏感。

与基线的比较更关键：全部 40 次运行中{\heiti 没有任何一次在收尾时间或总能耗上优于 24 架次基线}——各法最优收尾均在 7389~s 以上（基线 6276.83~s），各法最优能耗均在 74.04~kWh 以上（基线 71.9169~kWh）；搜索只在架次数这一维度上可以更少（最少 22 架次），代价是更长的收尾时间与更高的能耗。因此 24 架次基线在收尾—能耗平面上支配全部搜索解，架次维度上则存在“以时间和能耗换架次”的折中。该比较只在上述候选上限、墙钟保护与容忍规则下成立，不能据此比较四种方法在其他设置下的优劣。

\subsection{问题二主方案与结果分析}

\subsubsection{方案总体指标}

上述 24 架次基线来自此前归档的局部爬山方案。经统一物理模型、硬时限与资源占用独立核验后，取为问题二主方案，并作为问题三的运输起点：24 个运输架次完成 80 箱投送，全部运输无人机于 6276.83~s（104.61~min）返航，总能耗 71.9169~kWh，优先加权平均送达时刻 45.69~min，最后一箱于 5734.23~s 交付；按机型统计，A 型机执行 11 架次、B 型机 7 架次、C 型机 6 架次，14 组共享电池全部投入使用。逐箱核验确认医疗 16 箱、首批 30 箱、去重后 31 箱硬时限货箱全部按时送达，64 箱非医疗物资的加权迟到为 0；分服务区情况见表~\ref{tab:q2_service_delivery}，其中 S010 的最小提前量 221.03~s 为全区最小，是时限约束最紧的服务区。本节全部数值由随附程序独立核验得到，程序实现过程中使用了人工智能工具辅助，工具信息见参考文献\cite{codexai}，使用范围与后续处理策略见\hyperref[app:ai]{附录~B}；建模思路、指标取舍与结论均由参赛队独立确定。

\input{tables/q2_service_delivery}

\subsubsection{资源占用核验}

对 8 架实体机与 14 组共享电池逐一日历核查后确认：任意两架次对同一机体的占用区间不重叠，对同一电池的使用与充电区间亦不重叠；各架次返航荷电状态均不低于 20\% 下限，最低返航 SOC 为 20.4065\%。这一核验把式~\eqref{eq:q2-turnover}在解上逐条回代，用于确认机体与电池可以按该时间表复用。

\subsubsection{方案结果与指标分析}

按机型统计，A 型机承担 11 个架次（每架次 1～3 箱），B 型机 7 个架次（每架次 2～3 箱），C 型机因额定载重与可用容积最大，承担 6 个重载架次（每架次 3～8 箱），其目的服务区集中在 S001～S005，与附件中这些服务区的箱重分布一致。当前方案的多点运输航线、运输机排程、运输电池占用与充电区间、逐航次能耗、80 个货箱送达时刻与时限对照以及逐架次返航剩余电量依次见图~\ref{fig:routes}--\ref{fig:q2_soc}：图~\ref{fig:routes} 给出 24 架次的多点运输航线；图~\ref{fig:gantt} 以甘特条形给出 8 架机体在 0～104.61~min 内的复用关系与接续间隔；图~\ref{fig:q2_battery} 给出电池使用与充电的时间占用；图~\ref{fig:q2energy} 给出逐航次能耗；图~\ref{fig:q2_deadline_plot} 把 80 个货箱的实际送达时刻与时限逐箱对照；图~\ref{fig:q2_soc} 给出各架次返航剩余电量与 20\% 下限的相对位置。

\begin{figure}[H]\centering\includegraphics[width=.82\textwidth]{../figures/paper_full_20260926/q2_transport_routes.pdf}\caption{当前24架次方案的多点运输航线}\label{fig:routes}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=.88\textwidth]{../figures/paper_full_20260926/q2_drone_timeline.pdf}\caption{当前方案的运输机排程}\label{fig:gantt}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=.88\textwidth]{../figures/paper_full_20260926/q2_battery_timeline.pdf}\caption{当前方案的运输电池占用与充电区间}\label{fig:q2_battery}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=.90\textwidth]{../figures/paper_full_20260926/q2_route_energy.pdf}\caption{当前方案的逐航次能耗}\label{fig:q2energy}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=.78\textwidth]{../figures/paper_full_20260926/q2_delivery_deadlines.pdf}\caption{80个货箱的实际送达时刻与时限对照}\label{fig:q2_deadline_plot}\end{figure}
\begin{figure}[H]\centering\includegraphics[width=.92\textwidth]{../figures/paper_full_20260926/q2_return_soc.pdf}\caption{当前方案逐架次返航剩余电量}\label{fig:q2_soc}\end{figure}

四个指标的取舍关系如下。第一，及时性优先：$\bar{L}_\pi=0$ 说明 64 箱非医疗物资全部在期望时刻前送达，方案并非靠牺牲普通物资换取关键箱，而是在两者都不违约的前提下取得。第二，这一及时性以收尾时间与架次数为代价：24 架次、104.61~min 收尾，明显长于问题一单点直送、忽略实体资源时的 18 架次基准。第三，能耗不是独立优化对象：$\gamma_E=0.004065$ 由架次 T010（A 型机，路线 S010$\to$S009$\to$S005，3 箱，耗电约 3.582~kWh，返航 SOC 20.41\%）决定，该架次仅高出 20\% 返航储备 0.4065 个百分点，说明方案在能耗维度上几乎贴住安全边界，属“可行但裕度很小”的情形——这也是问题四讨论资源冗余与缺口的起点。

\subsection{方案验证与适用范围}

本文对问题二做三项检验：一是独立复算，由随附程序对 24 个架次重算航段耗时、逐段扣重能耗、交接时刻与返航 SOC，并与求解输出逐项比对；二是约束回代，把式~\eqref{eq:q2-assign}--\eqref{eq:q2-capacity}、式~\eqref{eq:q2-turnover}与硬截止 $T_b^{\rm arr}\le D_b$ 代入解中并输出松弛量；三是资源日历检验，核查机体与电池占用区间是否重叠，以及充电是否完成。所报方案是归档基线经独立验证后的可执行方案。本次搜索没有在收尾时间或总能耗上优于该基线，因此不把它写成完整车辆路径问题的全局最优。墙钟限制下的启发式不保证在不同机器上重新搜索得到同一候选，因此随附程序将已归档方案的独立复算与重新搜索分开。可行性以题设参数的确定值为前提，风场、临时禁飞区、交接工位数量与链路实测误差均未纳入，实际执行前需重新核验。与相邻子问题的衔接上，问题二与问题一共用式~\eqref{eq:range}--\eqref{eq:energy}的航段模型与返航储备，两问能耗可比；问题三以上述 24 架次方案为运输起点。为满足连续通信要求，运输结构、机型和时序有所调整，并新增中继任务。架次、收尾时间和能耗的综合差额见第7章，这里不再重复展开。
